# ATTI 用户使用说明

## 1. 这是什么

ATTI 是一个 Edge 扩展，用来帮助你在当前受支持的人格测评页面上减少重复答题。

当前稳定支持范围只有一个站点：

- `Truity`
- `Enneagram Personality Test`
- 目标地址族：`https://www.truity.com/test/enneagram-personality-test`

它现在可以帮助你完成这些事情：

- 在扩展里保存一份本地画像草稿
- 识别当前是否处于受支持的 Truity 测评页面
- 基于你的画像和当前题目生成 AI 推荐
- 在 side panel 中展示推荐结果、置信度和理由
- 在你主动触发后，把推荐答案自动填入页面

它现在不会做这些事情：

- 不支持第二个站点
- 不支持其他 Truity 测试
- 不会自动提交问卷
- 不会绕过登录、验证码或付费墙

## 2. 数据与隐私

默认保留在本地的数据包括：

- 你的本地画像草稿
- 已提取的问题
- 推荐答案
- 本地会话记录
- 诊断信息

只有在你主动点击 `开始 AI 规划` 时，系统才会按当前设置调用 provider。

当前真实试用主路径是：

- `OpenAI`

本地回退 provider 仍然存在，但它主要用于开发或受控演示，不是当前面向真实试用的主路径。

请记住两条规则：

- ATTI 可能会自动填入答案
- ATTI 不会自动提交问卷

## 3. 开始前准备

使用前请确认：

1. 你已经在 Edge 中加载了这个扩展。
2. 你打开的是 Truity Enneagram 测评问卷页面。
3. 你已经在 `ATTI 设置` 中选择了 `OpenAI（主路径）`。
4. 你已经保存了可用的 OpenAI API key。

如果没有保存 API key，popup、options 和 side panel 都会提示当前 provider 尚未就绪。

## 4. 当前版本怎么用

### 第一步：配置 provider

打开扩展的 `ATTI 设置` 页面，然后完成：

1. 在 `当前规划引擎` 中选择 `OpenAI（主路径）`
2. 在 `OpenAI API key` 输入框中填入 key
3. 确认页面出现 `OpenAI API key 已保存在本地。`

如果配置正确，你还会看到当前 AI 已就绪的状态说明。

### 第二步：进入受支持页面

在浏览器中打开：

- `https://www.truity.com/test/enneagram-personality-test`

然后打开 ATTI 的 side panel。

如果页面受支持，你会看到：

- 已识别页面：`truity-enneagram`
- 当前会话已经建立
- 题目已经提取完成

如果没有识别成功，请先确认你打开的是 Truity Enneagram 的答题页面，而不是结果页、账号页或其他 Truity 页面。

### 第三步：保存本地画像草稿

在 side panel 中填写：

- `画像摘要`
- `证据备注`

然后点击：

- `保存本地画像草稿`

这一步只会把你的画像草稿保存在本地。

### 第四步：开始 AI 规划并自动填入

在 side panel 中点击：

- `开始 AI 规划`

当前版本里，这个按钮就是唯一的显式用户触发。点击之后会连续发生三件事：

1. 生成推荐答案
2. 展示推荐预览
3. 将可用推荐自动填入当前问卷页面

也就是说，当前锁定的试用策略是：

- 你点击 `开始 AI 规划`，就等于确认触发本次填充
- 系统会在规划完成后立即执行 fill
- 但仍然不会自动 submit

### 第五步：查看推荐结果

side panel 会展示每道题的：

- 页面填写结果
- 置信度
- 理由说明

你可以把它理解为“已经用于页面填充的推荐摘要”。

## 5. 常见异常说明

### 1. provider 尚未就绪

如果没有保存 OpenAI key，或者 provider 配置不完整：

- `开始 AI 规划` 会被阻止，或出现明确提示
- 你需要先回到 `ATTI 设置` 完成配置

### 2. provider 请求失败

如果 OpenAI 调用失败，side panel 会显示清晰的错误提示。

这时通常需要检查：

- API key 是否正确
- 网络是否可用
- OpenAI 是否返回异常状态

### 3. 推荐质量不足

如果系统判断当前 recommendation 质量退化，例如：

- 置信度过低
- 理由像占位文本

那么 side panel 仍可能展示这些 recommendation，但系统不会把它们当成可安全执行的 fill 结果继续写入页面。

### 4. 页面无法被正确识别

如果页面结构发生变化，ATTI 可能无法正常提取或填充。

当前版本只对锁定的 Truity Enneagram 路径做了适配，不保证支持其他页面结构。

## 6. 当前版本边界

为了避免误解，请按下面这几个事实理解当前版本：

1. 这是一个单站点 MVP，不是通用网页自动答题工具。
2. 当前产品方向已经开始朝 AI-first 多站点演进，但今天真正稳定可运行的仍然只有 Truity Enneagram。
3. 当前发布状态是“小范围试用可发布”，不是更广泛的正式发布。
4. 仍然有延期项没有完成，例如更广泛 rollout 前的持续在线验证。

如果你只想快速记住当前怎么用，记住这三句话就够了：

1. 现在只支持 `Truity Enneagram`。
2. 点击 `开始 AI 规划` 会生成并填入答案，但不会自动提交。
3. 本地数据默认留在设备上，只有在你主动开始规划时才会调用 provider。
