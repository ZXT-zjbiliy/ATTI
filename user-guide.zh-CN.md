# ATTI 用户使用说明

## 1. 这是什么

ATTI 是一个 Edge 扩展，用来帮助你在当前受支持的测试网站问卷页上减少重复答题。

当前版本不是“任意网站自动填写工具”，也不是“所有测试网站都支持”的产品。  
它现在处于 `多测试网站小范围试用` 阶段，当前明确支持的范围只有这两个公开测试网站路由：

- `Truity / Enneagram Personality Test`
  - `https://www.truity.com/test/enneagram-personality-test`
- `16Personalities / Free Personality Test`
  - `https://www.16personalities.com/free-personality-test`

ATTI 现在可以帮助你完成这些事：

- 在扩展里保存一份本地画像草稿
- 识别当前页面是否属于受支持的测试网站问卷页
- 从当前页面提取问题
- 基于你的画像和当前问题生成 AI 推荐
- 在 side panel 中展示推荐结果、置信度和理由
- 在你主动触发后，把可用推荐自动填写到页面

ATTI 现在不会做这些事：

- 不支持任意网站自动填写
- 不支持非测试类网站
- 不支持未明确接入的其他测试网站或其他页面路由
- 不会自动提交问卷
- 不会绕过登录、验证码、付费墙或反机器人拦截

## 2. 当前版本的试用范围

当前版本的范围请按下面理解：

- `Truity` 是当前更成熟、信心更高的试用路径
- `16Personalities` 已经进入第二测试网站的小范围试用范围，但仍然是一个较窄的第二样本
- 这不代表产品已经进入更广泛正式发布
- 这也不代表产品已经支持所有测试网站

如果你不在上述两个页面路由内，ATTI 不应被理解为“应该也能自动工作”。

## 3. 数据与隐私

默认保留在本地的数据包括：

- 你的本地画像草稿
- 已提取的问题
- 推荐答案
- 本地会话记录
- 诊断信息

只有在你主动点击 `开始 AI 规划` 时，系统才会按当前设置调用 provider。

当前真实试用主路径是：

- `OpenAI`

本地回退 provider 仍然存在，但主要用于开发或受控演示，不是当前面向真实试用的主路径。

请记住两条规则：

- ATTI 可能会在你主动触发后自动填写答案
- ATTI 不会自动提交问卷

## 4. 开始前准备

使用前请确认：

1. 你已经在 Edge 中加载了这个扩展。
2. 你打开的是当前受支持的测试网站问卷页面。
3. 你已经在 `ATTI 设置` 中选择了 `OpenAI（主路径）`。
4. 你已经保存了可用的 `OpenAI API key`。

如果没有保存 API key，popup、options 和 side panel 都会提示当前 provider 尚未就绪。

## 5. 当前版本怎么用

### 第一步：配置 provider

打开扩展的 `ATTI 设置` 页面，然后完成：

1. 在 `当前规划引擎` 中选择 `OpenAI`
2. 在 `OpenAI API key` 输入框中填入 key
3. 确认页面出现 `OpenAI API key 已保存在本地。`

如果配置正确，你还会看到当前 AI 已就绪的状态说明。

### 第二步：进入受支持页面

在浏览器中打开以下任一页面：

- `https://www.truity.com/test/enneagram-personality-test`
- `https://www.16personalities.com/free-personality-test`

然后打开 ATTI 的 `side panel`。

如果页面受支持，你会看到类似信息：

- `已识别页面：truity-enneagram`
  或
- `已识别页面：sixteen-personalities`

同时会话状态会显示页面已经被识别并开始提取问题。

如果没有识别成功，请先确认你打开的是支持范围内的问卷页，而不是结果页、账号页、营销页或其他无关页面。

### 第三步：保存本地画像草稿

在 side panel 中填写：

- `画像摘要`
- `证据备注`

然后点击：

- `保存本地画像草稿`

这一步只会把你的画像草稿保存到本地。

### 第四步：开始 AI 规划并自动填写

在 side panel 中点击：

- `开始 AI 规划`

当前版本里，这个按钮就是主要的用户确认动作。点击之后会连续发生三件事：

1. 生成推荐答案
2. 展示推荐预览
3. 将可用推荐自动填写到当前问卷页面

当前试用策略可以理解为：

- 你点击 `开始 AI 规划`，就等于确认触发本次填写
- 系统会在规划完成后立即执行 fill
- 但仍然不会自动 submit

## 6. 你会看到什么

side panel 会展示每道题的：

- 页面填写结果
- 置信度
- 理由说明

如果某些 recommendation 被判定为质量降级，你仍然可能看到它们出现在预览中，但它们不会被当作可安全执行的 fill 结果继续写入页面。

## 7. 常见异常说明

### 1. provider 尚未就绪

如果没有保存 OpenAI key，或者 provider 配置不完整：

- `开始 AI 规划` 会被阻止，或出现明确提示
- 你需要先回到 `ATTI 设置` 完成配置

### 2. provider 请求失败

如果 OpenAI 调用失败，side panel 会显示清晰的错误提示。

这时通常需要检查：

- API key 是否正确
- 网络是否可用
- OpenAI 是否返回异常状态

### 3. 推荐质量不足

如果系统判断当前 recommendation 质量退化，例如：

- 置信度过低
- 理由像占位文本

那么 side panel 仍可能展示这些 recommendation，但系统不会把它们当成可安全执行的 fill 结果继续写入页面。

### 4. 页面无法被正确识别

如果页面结构发生变化，ATTI 可能无法正常提取或填写。

当前版本只对已接入的两个测试网站目标路由做了适配，不保证支持同域其他页面，也不保证支持未接入网站。

另外要注意：

- `Truity` 当前真实站点信心更高
- `16Personalities` 当前在本环境下仍缺少持续 live-site 验证，因为会受到 Cloudflare 限制

## 8. 当前版本边界

为了避免误解，请按下面这些事实理解当前版本：

1. 这不是任意网站通用自动填写工具。
2. 这不是“所有测试网站都支持”的产品。
3. 当前是“多测试网站小范围试用发布”状态，不是更广泛正式发布。
4. 当前明确支持的只有两个公开测试网站问卷路由：`Truity Enneagram` 和 `16Personalities Free Personality Test`。
5. `provider / storage / adapter / UI` 边界仍然保持分离，ATTI 不是靠 provider 直接理解网页 DOM 来工作的。
6. `no auto-submit` 仍然成立：可以自动 fill，但不会自动提交问卷。
7. 仍然有显式延期项没有完成，比如更广泛 rollout 前的持续在线验证。

## 9. 还没有覆盖的内容

当前版本仍然没有覆盖：

- 其他未接入测试网站
- 已接入域名下的其他无关页面或其他测试
- 非测试类网站
- 通用网页登录、支付、招聘、表单后台等页面
- 云同步
- 多 profile 协作
- 巨型审核台
- 权限中心

## 10. 如果你只想记住三句话

1. 现在只支持两个明确接入的测试网站路由：`Truity Enneagram` 和 `16Personalities Free Personality Test`。
2. 点击 `开始 AI 规划` 会生成并填写答案，但不会自动提交。
3. 数据默认保留在本地，只有在你主动开始规划时才会调用 provider。
