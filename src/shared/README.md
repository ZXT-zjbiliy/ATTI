# Shared Module

This directory contains shared cross-runtime contracts.

Subdirectories:

- `types/`: shared types
- `schemas/`: validation schemas
- `utils/`: narrowly scoped shared helpers only

Avoid turning `utils/` into a generic dumping ground.
