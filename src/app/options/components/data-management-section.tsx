import { OptionsSection } from "./options-section";

export function DataManagementSection() {
  return (
    <OptionsSection title="Data Management">
      <p>Profile, session, and recommendation management will appear here in a future milestone.</p>
      <p>No destructive data actions are available in this shell.</p>
    </OptionsSection>
  );
}
