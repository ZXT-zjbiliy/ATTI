# App Module

This directory is reserved for extension UI runtimes only.

Subdirectories:

- `popup/`: compact launcher/status UI
- `sidepanel/`: primary workflow UI
- `options/`: configuration and debug UI

No page DOM automation, raw storage access, or provider implementation should live here.
