# LLM Module

This directory is reserved for model integration boundaries.

Subdirectories:

- `providers/`: concrete provider implementations
- `prompts/`: prompt construction
- `parsers/`: structured response parsing

No UI components or raw page automation logic should live here.
